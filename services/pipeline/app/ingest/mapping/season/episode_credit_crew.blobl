root = this.data.episodes.
  map_each(e -> e.crew.
    map_each(c -> c.
      with("department", "job").
      assign({
        "credit_id" : "00000000%s".format(c.credit_id),
        "version_ts": this.meta.ts,
      })
    )
  ).
  flatten()
